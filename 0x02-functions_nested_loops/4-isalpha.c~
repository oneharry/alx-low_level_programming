
#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	return (0);
		
}
int main(void)
{
	int num = 0;
	
	printf("Enter: ");
	scanf("%d", &num);
	printf("%d", _islower(num));
	return (0);
}
