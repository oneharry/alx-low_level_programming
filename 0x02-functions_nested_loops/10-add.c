#include <stdio.h>
#include "main.h"
/**
 * add - Return the sum of two numbers
 * @x: first aegument integer
 * @y: second argument integer
* Return: return an integer
 */
int add(int x, int y)
{
	int sum = x + y;
	return (sum);
}

