#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char msg[] = "Hello WOrld";
	char *str = msg;
	int i = 0;
	while (*str != '\0')
	{
		putchar(*str);
		*str++;
	}
	return (0);
}
