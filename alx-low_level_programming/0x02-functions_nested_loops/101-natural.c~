#include <stdio.h>
#include "main.h"
/**
 * print_to_98 - Return the sum of two numbers
 * @x: the integer of starting point to print
* Return: void
 */
int main(void)
{
	int x, sum;
	for (x = 0; x  < 1024; x++)
	{
		if ((x % 3) == 0 || (x % 5) == 0)
			sum++;
	}
	printf("%d\n", sum);
	return (0);
}
