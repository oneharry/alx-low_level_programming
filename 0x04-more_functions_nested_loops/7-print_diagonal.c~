#include "main.h"

/**
 * print_line - Prints numbers 0-14 in 10 places
 * @n: print n numbers of line
 * Return: void
 */

void print_line(int n)
{
	int x;

	if (n > 0)
	{
		for (x = 0; x < n; n++)
		{
			_putchar('_');
		}
	}
	_putchar('\n');
}
