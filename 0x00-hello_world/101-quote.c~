#include <stdio.h>

/**
 * main - Entry point
 * Output - Prints strings to the out there
 * Return: Always 0 (Success)
 */
char message(void)
{
	return 'I am me';
}
int main(void)
{
	message();
	printf("Here");
	return 0;
}
