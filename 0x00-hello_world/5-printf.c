#include <stdio.h>

/**
 * main - Entry point
 * Output - Prints some text to the outup
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
