#include "main.h"
/**
  * strtow - splits word in a string
  * @str: pointer to string
  * Return: returns pointer to pointer arrays
  */
char **strtow(char *str)
{

	if (*str == ' ' || str == NULL)
		return (NULL);
	return (NULL);
}
