#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry Point
 * Return: Always 0 (Success)
 */
int main(void)
{
	unsigned int x;

	for (x = 0; x < 10; x++)
	{
		printf("%u\n", x);
	}
		return (0);
}
